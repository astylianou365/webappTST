name: Trigger auto deployment for ia43rg05contapps02

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/ia43rg05contapps02-AutoDeployTrigger-ff80b6e5-58cf-42bb-b177-60cae8e19dae.yml'

  # Allow manual trigger 
  workflow_dispatch:
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.IA43RG05CONTAPPS02_AZURE_CREDENTIALS }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: ia43rg05acr01.azurecr.io
          registryUsername: ${{ secrets.IA43RG05CONTAPPS02_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.IA43RG05CONTAPPS02_REGISTRY_PASSWORD }}
          containerAppName: ia43rg05contapps02
          resourceGroup: IA43-RG05
          imageToBuild: ia43rg05acr01.azurecr.io/ia43rg05contapps02:${{ github.sha }}
          
            

     

